#EVENT {RECEIVED LINE}
{
	#if {"%0" != "+MSG If you are interested in this advertisement, press Control+W now to get more information."}
	{
		#variable {ad.lastLine} {%0}
	};
	#else
	{
		#line {gag};
		#system {echo "#gag {^${ad.lastLine}}" >> "$HOME/ads.txt"};
		#read {$HOME/ads.txt}
	}
}
