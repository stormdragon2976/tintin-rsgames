#ACTION {^+MSG It is your turn.$}
{
	#nop {This replaces the same action from the rs.tin file, so it needs to be 8 or higher to take presidence.};
	#if {"${game.name}" == "1000 miles" && "${auto.draw}" == "true"}
	{
		#send {Draw card}
	};
	#system {play -qV0 "|sox -np synth tri C6 fade 0 0.02 0.01" repeat 6 &> /dev/null&}
}
{8}
